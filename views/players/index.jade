extends ../layout

block title
  title= "Players - " + settings.title

block content
  p Checked players are considered active for the day, and are included when
    | generating random teams.
  form(method='post')
    ul.no-bullet
      each player in players
        li
          label
            input(checked=player.isActive name='activePlayers' type='checkbox' value=player.name)
            span= player.name
    p: input(type='submit' value='Save & Generate Teams')
